{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "python3 server.py",
    "start": "python3 server.py",
    "build": "echo 'No-op build for server-side Python App'",
    "lint": "echo 'No linting for vanilla JS/Python yet'"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  },
  "devDependencies": {
  }
}
